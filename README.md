# java-batch-event-processor
A simple Java-based batch event processing application that reads structured event data, validates records, aggregates results, and prints summary reports to the console.
